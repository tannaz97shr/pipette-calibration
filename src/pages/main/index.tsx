import Flow from "./flow";

import Styles from "./styled";

const MainPage = () => {
  const { PageContainer } = Styles;
  return (
    <PageContainer>
      <Flow />
    </PageContainer>
  );
};

export default MainPage;
